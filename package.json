{
	"name": "implement",
	"private": true,
	"version": "1.0.0",
	"description": "",
	"type": "module",
	"scripts": {
		"dev": "pnpm --filter @apps/docs dev",
		"build": "pnpm -r build",
		"check": "tsc --noEmit -p tsconfig.scripts.json && pnpm -r check",
		"test": "NO_COLOR=1 pnpm -r test",
		"generate": "pnpm -r generate",
		"registry": "pnpm -r registry",
		"registry:publish": "pnpm -r registry:publish",
		"skills": "pnpm --filter @apps/docs skills",
		"create:app": "pnpm --filter create-implement-app build && node packages/create-implement-app/dist/bin.mjs",
		"jscpd": "jscpd --no-tips",
		"lint": "oxlint --disable-nested-config --deny-warnings",
		"lint:fix": "oxlint --disable-nested-config --fix --deny-warnings",
		"format": "oxfmt",
		"format:check": "oxfmt --check",
		"check:changesets": "node --experimental-strip-types scripts/check-changesets.ts",
		"changeset": "changeset",
		"changeset:version": "changeset version && pnpm --filter @apps/docs sync && pnpm --filter @apps/docs registry",
		"changeset:publish": "pnpm build && changeset publish",
		"prepare": "git config core.hooksPath .githooks || exit 0"
	},
	"keywords": [],
	"author": "",
	"license": "ISC",
	"packageManager": "pnpm@11.22.0",
	"devDependencies": {
		"@changesets/changelog-github": "^1.0.0",
		"@changesets/cli": "^3.0.1",
		"@implementjs/eslint": "workspace:*",
		"@types/node": "^26.2.0",
		"jscpd": "^5.0.15",
		"oxfmt": "^0.63.0",
		"oxlint": "^1.78.0",
		"oxlint-tsgolint": "^7.0.2001",
		"typescript": "^7.0.2"
	}
}
