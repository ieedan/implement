/**
 * `$implement/schema-converters`, generated per app by kit's own Vite plugin —
 * see `generateConvertersModule` in `./codegen.ts`. It exists only inside a kit
 * build, so this declaration is what lets `./openapi.ts` name it; the import is
 * written to survive its absence everywhere else.
 */
declare module "$implement/schema-converters" {
	export const converters: Record<string, Record<string, unknown>>;
}
