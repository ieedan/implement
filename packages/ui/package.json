{
	"name": "@packages/ui",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"exports": {
		".": {
			"types": "./src/index.ts",
			"default": "./src/index.ts"
		}
	},
	"scripts": {
		"dev": "tsdown --watch",
		"build": "tsdown",
		"check": "tsc --noEmit && tsc --noEmit -p tsconfig.scripts.json"
	},
	"dependencies": {
		"fast-deep-equal": "^3.1.3"
	},
	"devDependencies": {
		"@types/node": "^26.2.0",
		"tsdown": "^0.22.14",
		"typescript": "^7.0.2"
	}
}
